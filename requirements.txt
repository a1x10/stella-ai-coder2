rich>=13.7.0
requests>=2.31.0
pyautogui>=0.9.54
pillow>=10.0.0
flask>=3.0.0
psutil>=5.9.8
pygetwindow>=0.0.9
pytesseract>=0.3.10

# Notes:
# - tkinter is installed via OS packages (python3-tk on Debian/Ubuntu), not pip.
# - Android phone control uses external adb/platform-tools when available.
# - Desktop Operator needs a real GUI session for mouse/keyboard/screenshot actions.
# - OCR needs the external Tesseract binary and language packs (for example: tesseract-ocr, tesseract-ocr-eng, tesseract-ocr-rus).
# - Linux window listing/focus works best with wmctrl/xdotool installed.
